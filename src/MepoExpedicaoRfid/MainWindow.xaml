<Window x:Class="MepoExpedicaoRfid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MAPLE DESKTOP RFID" Height="860" Width="1320"
        WindowStartupLocation="CenterScreen">

  <Grid Background="#F3F4F6">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="280"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!-- Sidebar Navigation -->
    <Border Grid.Column="0" Background="#111827" Padding="16">
      <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
          <Border Background="#FFFFFF" CornerRadius="12" Padding="10" Margin="0,0,0,12">
            <Image Source="Assets/maple-logo.png" Height="44" Stretch="Uniform" HorizontalAlignment="Left"/>
          </Border>
          <TextBlock Text="MAPLE DESKTOP RFID" FontSize="18" FontWeight="Bold" Foreground="White"/>
          <TextBlock Text="Leitura RFID (R3)" Opacity="0.75" Margin="0,4,0,0" Foreground="White"/>
        </StackPanel>

        <!-- Menu Items -->
        <StackPanel DockPanel.Dock="Top">
          <Button Command="{Binding Nav.Dashboard}" Content="📊 Dashboard" Padding="12,10" Background="#16A34A" Foreground="White" FontSize="14" Margin="0,0,0,8" Cursor="Hand"/>
          <Button Command="{Binding Nav.Saida}" Content="📤 Saída (Expedição)" Padding="12,10" Background="#16A34A" Foreground="White" FontSize="14" Margin="0,0,0,8" Cursor="Hand"/>
          <Button Command="{Binding Nav.Entrada}" Content="📥 Entrada (Recebimento)" Padding="12,10" Background="#16A34A" Foreground="White" FontSize="14" Margin="0,0,0,8" Cursor="Hand"/>
          <Button Command="{Binding Nav.Fila}" Content="📋 Fila de Expedição" Padding="12,10" Background="#16A34A" Foreground="White" FontSize="14" Margin="0,0,0,8" Cursor="Hand"/>
          <Button Command="{Binding Nav.Consulta}" Content="🔍 Consulta de Tags" Padding="12,10" Background="#16A34A" Foreground="White" FontSize="14" Margin="0,0,0,8" Cursor="Hand"/>
          <Button Command="{Binding Nav.Config}" Content="⚙️ Configuração" Padding="12,10" Background="#16A34A" Foreground="White" FontSize="14" Cursor="Hand"/>
        </StackPanel>

        <TextBlock Text="" DockPanel.Dock="Bottom" Margin="0,8,0,0"/>
        <Border DockPanel.Dock="Bottom" Background="#0B1220" Padding="12" CornerRadius="10">
          <StackPanel>
            <TextBlock Text="Status do Hardware" FontSize="12" FontWeight="Bold" Foreground="White" Margin="0,0,0,8"/>
            <TextBlock x:Name="HardwareStatus" Text="✓ Hardware RFID R3 Conectado" FontSize="11" Foreground="#4FD1C5" TextWrapping="Wrap"/>
            <TextBlock x:Name="ReaderInfo" Text="DLL: UHFAPI.dll v3.x - Pronto para leitura" FontSize="10" Opacity="0.7" Foreground="White" Margin="0,4,0,0" TextWrapping="Wrap"/>
          </StackPanel>
        </Border>
      </DockPanel>
    </Border>

    <!-- Main Content Area - Dynamic Navigation -->
    <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Background="White"/>
  </Grid>
</Window>
