<UserControl x:Class="MepoExpedicaoRfid.Views.DashboardView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                 mc:Ignorable="d">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <TextBlock Text="Dashboard" Style="{StaticResource H1}" Margin="0,0,0,16"/>

          <Grid Grid.Row="1">

<Grid>
  <Grid.ColumnDefinitions>
    <ColumnDefinition Width="1*"/>
    <ColumnDefinition Width="1*"/>
    <ColumnDefinition Width="1*"/>
  </Grid.ColumnDefinitions>

  <Border Style="{StaticResource CardBorder}" Grid.Column="0">
    <StackPanel>
      <TextBlock Text="Tags únicas (sessão)" Style="{StaticResource Muted}"/>
      <TextBlock Text="{Binding TotalTags}" FontSize="36" FontWeight="Bold" Margin="0,6,0,0"/>
      <TextBlock Text="{Binding ReadsPerSecond}" Opacity="0.7" Margin="0,6,0,0"/>
    </StackPanel>
  </Border>

  <Border Style="{StaticResource CardBorder}" Grid.Column="1" Margin="12,0,12,12">
    <StackPanel>
      <TextBlock Text="Últimas tags" Style="{StaticResource Muted}"/>
      <ItemsControl ItemsSource="{Binding LastTags}" Margin="0,10,0,0">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding}" FontFamily="Consolas" Opacity="0.85" Margin="0,2,0,2"/>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </StackPanel>
  </Border>

  <Border Style="{StaticResource CardBorder}" Grid.Column="2">
    <StackPanel>
      <TextBlock Text="Dicas rápidas" Style="{StaticResource Muted}"/>
      <TextBlock Text="• Abra a FILA para puxar o próximo pedido" Margin="0,10,0,0" Opacity="0.85"/>
      <TextBlock Text="• Na SAÍDA você vê Tags / SKUs / Lotes em tempo real" Margin="0,6,0,0" Opacity="0.85"/>
      <TextBlock Text="• Consulta Tag mostra histórico completo no Supabase" Margin="0,6,0,0" Opacity="0.85"/>
    </StackPanel>
  </Border>
</Grid>

          </Grid>
        </Grid>
      </ScrollViewer>
    </UserControl>
