<UserControl x:Class="MepoExpedicaoRfid.Views.FilaView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                 mc:Ignorable="d">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <TextBlock Text="Fila de Expedição" Style="{StaticResource H1}" Margin="0,0,0,16"/>

          <Grid Grid.Row="1">

<Grid>
  <Grid.ColumnDefinitions>
    <ColumnDefinition Width="1*"/>
    <ColumnDefinition Width="1*"/>
    <ColumnDefinition Width="1*"/>
  </Grid.ColumnDefinitions>

  <StackPanel Grid.Column="0">
    <DockPanel Margin="0,0,0,10">
      <TextBlock Text="Pendentes" FontWeight="SemiBold" DockPanel.Dock="Left"/>
      <Button Content="Atualizar" Command="{Binding Refresh}" Padding="8,4" DockPanel.Dock="Right"/>
    </DockPanel>

    <ItemsControl ItemsSource="{Binding Pendentes}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardBorder}">
            <StackPanel>
              <TextBlock Text="{Binding NumeroPedido}" FontSize="18" FontWeight="SemiBold"/>
              <TextBlock Text="{Binding Cliente}" Opacity="0.75" Margin="0,4,0,0"/>
              <DockPanel Margin="0,10,0,0">
                <TextBlock Text="{Binding TotalItens, StringFormat=Itens: {0}}" Opacity="0.75" DockPanel.Dock="Left"/>
                <TextBlock Text="{Binding Prioridade, StringFormat=Pri: {0}}" Opacity="0.75" DockPanel.Dock="Right"/>
              </DockPanel>
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>

  <StackPanel Grid.Column="1" Margin="12,0,12,0">
    <TextBlock Text="Em separação" FontWeight="SemiBold" Margin="0,0,0,10"/>
    <ItemsControl ItemsSource="{Binding EmSeparacao}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardBorder}">
            <StackPanel>
              <TextBlock Text="{Binding NumeroPedido}" FontSize="18" FontWeight="SemiBold"/>
              <TextBlock Text="{Binding Cliente}" Opacity="0.75" Margin="0,4,0,0"/>
              <TextBlock Text="{Binding TagsLidas, StringFormat=Tags lidas: {0}}" Opacity="0.75" Margin="0,8,0,0"/>
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>

  <StackPanel Grid.Column="2">
    <TextBlock Text="Finalizados" FontWeight="SemiBold" Margin="0,0,0,10"/>
    <ItemsControl ItemsSource="{Binding Finalizados}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardBorder}">
            <StackPanel>
              <TextBlock Text="{Binding NumeroPedido}" FontSize="18" FontWeight="SemiBold"/>
              <TextBlock Text="{Binding Cliente}" Opacity="0.75" Margin="0,4,0,0"/>
              <TextBlock Text="{Binding FinalizadoEm, StringFormat=Finalizado: {0:dd/MM HH:mm}}" Opacity="0.75" Margin="0,8,0,0"/>
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>
</Grid>

          </Grid>
        </Grid>
      </ScrollViewer>
    </UserControl>
